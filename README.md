# Clinical Monitoring Copilot (Prototype)

Protocol + patient dataset in → protocol deviations out.

## Quickstart
1. `cp .env.example .env` and add your Anthropic API key
2. `docker compose up --build`
3. Open http://localhost:3000
